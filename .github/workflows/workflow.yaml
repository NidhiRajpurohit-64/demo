name: CI

on: push
env:
 APP_NAME: my-app
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      MODE_ENV: production
    steps:
      - name: Show them
        env:
         GREETING: hello
        run: echo "$APP_NAME $MODE_ENV $GREETING"
