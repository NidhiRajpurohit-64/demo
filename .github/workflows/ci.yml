name: CI

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show the commit
        run: echo "Commit: $GITHUB_SHA"

      - name: List the files
        run: ls -la

