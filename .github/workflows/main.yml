jobs:
 deploy:
  runs-on: ubuntu-latest
  steps:
   - uses: action/checkout@v4
   - name: Deploy
   env:
    TOKEN: ${{ secrets.DEPLOY_TOKEN }}
   run:
    echo "Deploying with token length: ${#TOKEN}"
